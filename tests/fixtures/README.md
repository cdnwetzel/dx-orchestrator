Synthetic role cards.

These mirror the *structure* of `cdnwetzel/sdlc-agent-roles` cards — the inline
`**Agent fit:** … **9-person seat:** …` metadata line, the exact `##` section
headings — but the prose is invented. The real cards live in a separate repo and
must not be vendored here. Tests assert on parsing behavior, never on real
governance text.
